<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

 <mapper namespace = "com.ztgg.ecommerce.dao.UserDao">
 	<select id = "queryUser" resultType = "com.ztgg.ecommerce.entity.User">
 		SELECT user_id, name, profile_img, email, gender, enable_status, user_type, time_created, time_updated
 		FROM tb_user
 		ORDER BY user_id
 	</select>
 </mapper>